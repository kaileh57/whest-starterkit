name: Whest learned trajectory closure

on:
  pull_request:
    branches:
      - agent/whest-research
    paths:
      - "research/learned/**"
      - ".github/workflows/whest-learned.yml"

permissions:
  contents: read

jobs:
  train-mini:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    env:
      OMP_NUM_THREADS: "4"
      OPENBLAS_NUM_THREADS: "4"
      MKL_NUM_THREADS: "4"
      NUMEXPR_NUM_THREADS: "4"
      HF_HOME: ${{ github.workspace }}/.cache/huggingface
      HF_HUB_DISABLE_TELEMETRY: "1"
      PYTHONUNBUFFERED: "1"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install training dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "torch>=2.5,<2.9" "datasets>=4,<6" "pyarrow>=18" numpy scipy
          python -m pip check

      - name: Train and validate trajectory closure
        run: |
          python research/learned/train_trajectory.py \
            --dataset mini \
            --train-count 80 \
            --val-count 20 \
            --hidden 12 \
            --epochs 24 \
            --seed 20260809 \
            --output research/learned/artifacts/mini_v1

      - name: Show metrics
        if: always()
        run: |
          cat research/learned/artifacts/mini_v1/metrics.json 2>/dev/null || true
          cat research/learned/artifacts/mini_v1/training.log 2>/dev/null || true

      - name: Upload model and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: learned-trajectory-mini-${{ github.run_id }}
          path: research/learned/artifacts/mini_v1
          if-no-files-found: warn
          retention-days: 30
